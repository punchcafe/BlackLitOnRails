<nav class="admin_nav">
  <div class="logo">
    <%=image_tag "admin_logo.png" %>
  </div>
  <div class="admin_links">
    <%= link_to 'episodes', admin_episodes_path, class: @section == "episodes" ? "selected_link" : "" %>
    <%= link_to 'pages', admin_public_texts_path, class: @section == "public texts" ? "selected_link" : "" %>
    <%= link_to 'admins', admins_path, class: @section == "admins" ? "selected_link" : "" %>
  </div>
  <div class="current_admin">
    <i>Logged in: </i><b><%= @current_admin["email"] %></b><br>
    <%= link_to 'Log out', session_path, :method => :delete %>
  </div>
</nav>
<div class="nav-buffer"></div>
